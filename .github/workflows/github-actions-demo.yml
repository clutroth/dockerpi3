name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Dind action
        uses: ouzi-dev/dind-action@v1.0.1
      - run: docker login -u clutroth -p ${DOCKERHUB_TOKEN}
      - run: ./build
      - run: docker push